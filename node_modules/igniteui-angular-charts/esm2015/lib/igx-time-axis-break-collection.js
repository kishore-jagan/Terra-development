import { IgCollection } from "igniteui-angular-core";
import { markType } from "igniteui-angular-core";
import { SyncableObservableCollection$2 } from "igniteui-angular-core";
import { TimeAxisBreak as TimeAxisBreak_internal } from './TimeAxisBreak';
import { IgxTimeAxisBreakComponent as IgxTimeAxisBreakComponent } from './igx-time-axis-break-component';
export class IgxTimeAxisBreakCollection extends IgCollection {
    constructor(list) {
        super();
        if (!IgxTimeAxisBreakComponent.$type) {
            IgxTimeAxisBreakComponent.$type = markType(IgxTimeAxisBreakComponent, "IgxTimeAxisBreakComponent");
        }
        if (list) {
            for (let i = 0; i < list.length; i++) {
                this.add(list[i]);
            }
        }
    }
    _createInnerColl() {
        if (!IgxTimeAxisBreakComponent.$type) {
            IgxTimeAxisBreakComponent.$type = markType(IgxTimeAxisBreakComponent, "IgxTimeAxisBreakComponent");
        }
        let coll = new SyncableObservableCollection$2(IgxTimeAxisBreakComponent.$type, TimeAxisBreak_internal.$type, 0);
        coll.compare = (ext, int) => {
            let comp = ext;
            if (comp._implementation) {
                comp = comp._implementation;
            }
            if (comp.equals) {
                return comp.equals(int);
            }
            return comp === int;
        };
        coll.createTo = (ext) => {
            return ext._implementation;
        };
        coll.createFrom = (int) => {
            let ext = int.externalObject;
            if (!ext) {
                ext = new IgxTimeAxisBreakComponent();
                if (ext) {
                    if (!int.$type && ext._implementation.setNativeElement) {
                        ext._implementation.setNativeElement(int);
                    }
                    else {
                        ext._implementation = int;
                    }
                }
            }
            return ext;
        };
        return coll;
    }
}
