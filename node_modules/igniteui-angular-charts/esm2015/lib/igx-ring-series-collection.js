import { IgCollection } from "igniteui-angular-core";
import { markType } from "igniteui-angular-core";
import { SyncableObservableCollection$2 } from "igniteui-angular-core";
import { RingSeriesBase as RingSeriesBase_internal } from './RingSeriesBase';
import { IgxRingSeriesBaseComponent as IgxRingSeriesBaseComponent } from './igx-ring-series-base-component';
export class IgxRingSeriesCollection extends IgCollection {
    constructor(list) {
        super();
        if (!IgxRingSeriesBaseComponent.$type) {
            IgxRingSeriesBaseComponent.$type = markType(IgxRingSeriesBaseComponent, "IgxRingSeriesBaseComponent");
        }
        if (list) {
            for (let i = 0; i < list.length; i++) {
                this.add(list[i]);
            }
        }
    }
    _createInnerColl() {
        if (!IgxRingSeriesBaseComponent.$type) {
            IgxRingSeriesBaseComponent.$type = markType(IgxRingSeriesBaseComponent, "IgxRingSeriesBaseComponent");
        }
        let coll = new SyncableObservableCollection$2(IgxRingSeriesBaseComponent.$type, RingSeriesBase_internal.$type, 0);
        coll.compare = (ext, int) => {
            let comp = ext;
            if (comp._implementation) {
                comp = comp._implementation;
            }
            if (comp.equals) {
                return comp.equals(int);
            }
            return comp === int;
        };
        coll.createTo = (ext) => {
            return ext._implementation;
        };
        coll.createFrom = (int) => {
            let ext = int.externalObject;
            if (!ext) {
                ext = IgxRingSeriesBaseComponent._createFromInternal(int);
                if (ext) {
                    if (!int.$type && ext._implementation.setNativeElement) {
                        ext._implementation.setNativeElement(int);
                    }
                    else {
                        ext._implementation = int;
                    }
                }
            }
            return ext;
        };
        return coll;
    }
}
