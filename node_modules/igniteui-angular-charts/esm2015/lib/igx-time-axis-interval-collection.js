import { IgCollection } from "igniteui-angular-core";
import { markType } from "igniteui-angular-core";
import { SyncableObservableCollection$2 } from "igniteui-angular-core";
import { TimeAxisInterval as TimeAxisInterval_internal } from './TimeAxisInterval';
import { IgxTimeAxisIntervalComponent as IgxTimeAxisIntervalComponent } from './igx-time-axis-interval-component';
export class IgxTimeAxisIntervalCollection extends IgCollection {
    constructor(list) {
        super();
        if (!IgxTimeAxisIntervalComponent.$type) {
            IgxTimeAxisIntervalComponent.$type = markType(IgxTimeAxisIntervalComponent, "IgxTimeAxisIntervalComponent");
        }
        if (list) {
            for (let i = 0; i < list.length; i++) {
                this.add(list[i]);
            }
        }
    }
    _createInnerColl() {
        if (!IgxTimeAxisIntervalComponent.$type) {
            IgxTimeAxisIntervalComponent.$type = markType(IgxTimeAxisIntervalComponent, "IgxTimeAxisIntervalComponent");
        }
        let coll = new SyncableObservableCollection$2(IgxTimeAxisIntervalComponent.$type, TimeAxisInterval_internal.$type, 0);
        coll.compare = (ext, int) => {
            let comp = ext;
            if (comp._implementation) {
                comp = comp._implementation;
            }
            if (comp.equals) {
                return comp.equals(int);
            }
            return comp === int;
        };
        coll.createTo = (ext) => {
            return ext._implementation;
        };
        coll.createFrom = (int) => {
            let ext = int.externalObject;
            if (!ext) {
                ext = new IgxTimeAxisIntervalComponent();
                if (ext) {
                    if (!int.$type && ext._implementation.setNativeElement) {
                        ext._implementation.setNativeElement(int);
                    }
                    else {
                        ext._implementation = int;
                    }
                }
            }
            return ext;
        };
        return coll;
    }
}
