import { IgCollection } from "igniteui-angular-core";
import { SyncableObservableCollection$2 } from "igniteui-angular-core";
import { FinancialOverlayType_$type } from './FinancialOverlayType';
import { ensureEnum } from "igniteui-angular-core";
export class IgxFinancialOverlayTypeCollection extends IgCollection {
    constructor(list) {
        super();
        if (list) {
            for (let i = 0; i < list.length; i++) {
                this.add(list[i]);
            }
        }
    }
    _ensureOuter(item) {
        return ensureEnum(FinancialOverlayType_$type, item);
    }
    _createInnerColl() {
        let coll = new SyncableObservableCollection$2(FinancialOverlayType_$type, FinancialOverlayType_$type, 0);
        coll.compare = (ext, int) => {
            let comp = ext;
            if (comp.equals) {
                return comp.equals(int);
            }
            return comp === int;
        };
        coll.createTo = (ext) => {
            return ext;
        };
        coll.createFrom = (int) => {
            return int;
        };
        return coll;
    }
}
